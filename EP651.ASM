		PUBLIC	FIBN
FIBN 	PROC 	NEAR
		PUSH BP
 		MOV BP,SP
 		PUSH AX
 		PUSH BX 
 		MOV AX,[BP+4]
 		CMP AX,2
 		JA FIB0
 		MOV AX,1
 		JMP OVER 
FIB0:	DEC AX
 		MOV BX,AX
 		DEC BX
 		PUSH AX
 		CALL FIBN
 		POP AX
 		PUSH BX
 		CALL FIBN
 		POP BX
 		ADD AX,BX
OVER: 	MOV [BP+4],AX
 		POP BX
 		POP AX
 		POP BP
 		RET
FIBN 	ENDP