SSEG 	SEGMENT
STK 	DB 		50 DUP (0)
SSEG	ENDS
DSEG 	SEGMENT
TEXT	DB		'SDFDS'
CNT 	DB 		5
KEY 	DB		'BCD'
KCNT	DB 		3
RLT 	DB 		5 DUP (0)
DSEG 	ENDS
CSEG 	SEGMENT	
		ASSUME	CS:CSEG,DS:DSEG
START: 	MOV		AX,DSEG
		MOV 	DS,AX
		MOV 	AX,SSEG
		MOV 	SS,AX
		MOV 	SP,SIZE STK
		XOR 	CX,CX
		MOV 	CH,CNT
		MOV 	CL,KCNT
		LEA 	DI,TEXT
		LEA 	SI,KEY
		LEA 	BX,RLT
		PUSH 	DI
		PUSH 	SI
		PUSH 	BX
		CALL 	SECRT
		MOV 	AH,4CH
		INT 	21H
		INCLUDE	EP751.ASM
CSEG 	ENDS
		END 	START