		EXTRN 	PRIME:NEAR
		EXTRN 	QHTOA:NEAR
SSEG 	SEGMENT
STK 	DB 		50 DUP (0)
SSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,SS:SSEG
START:	MOV 	AX,SSEG
		MOV 	SS,AX
		MOV 	SP,SIZE STK
		XOR 	DX,DX
		MOV 	AL,2
LOOP1:	INC 	AL
		CALL 	PRIME
		JNC 	NEXT
		XOR 	AH,AH
		ADD 	DX,AX
NEXT:	CMP 	AL,255
		JB 		LOOP1
		MOV 	AX,DX
		CALL 	QHTOA
		PUSH 	AX
		MOV 	DL,BH
		MOV 	AH,02
		INT 	21H
		MOV 	DL,BL
		MOV 	AH,02
		INT 	21H
		POP 	BX
		MOV 	DL,BH
		MOV 	AH,02
		INT 	21H
		MOV 	DL,BL
		MOV 	AH,02
		INT 	21H
		MOV 	AH,4CH
		INT 	21H
CSEG 	ENDS
		END 	START