DSEG	SEGMENT
ADR 	DB		89H,01H,01H
RLT 	DB		?,?,?
DSEG 	ENDS
CSEG 	SEGMENT
		ASSUME	DS:DSEG,CS:CSEG
START:	MOV 	AX,DSEG
		MOV 	DS,AX
		; CMP 	ADR,80H
		; JAE 	NEGN
; NEGN:	
		MOV 	AL,ADR
		NEG 	AL
		MOV 	RLT,AL
		MOV 	AL,ADR+1
		NEG 	AL
		MOV 	RLT+1,AL
		MOV 	AL,ADR+2
		NEG 	AL
		MOV 	RLT+2,AL
		MOV 	AH,4CH
		INT 	21H
CSEG	ENDS
		END 	START 	
