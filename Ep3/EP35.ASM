DSEG	SEGMENT
DAX		DB		2
DAY		DB		3
RLT		DW		0
QTAB	DB		0,1,4,9,1,25,36,49,64,81,100
		DB		121,144,169,196,225
DSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG
START:	MOV		AX,DSEG
		MOV		DS,AX
		LEA		BX,QTAB
		XOR		AX,AX
		MOV		AL,DAX
		XLAT	
		MOV 	DX,AX
		MOV		AL,DAY
		XLAT
		ADD		AX,DX
		MOV 	RLT,AX
		MOV		AX,4CH
		INT		21H
CSEG	ENDS
		END 	START