DSEG	SEGMENT
QTAB	DB		0,0,0,0,1,2,3,4,6,8
HTAB 	DB		0,1,4,9,6,5,6,9,4,1
DSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG
START:	MOV		AX,DSEG
		MOV		DS,AX
		MOV 	AH,01H	;输入一个0~9的数字
		INT 	21H
		SUB 	AL,30H
		MOV 	CL,AL
		LEA 	BX,QTAB
		XLAT
		MOV 	CH,AL
		MOV 	DL,':'
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,CH
		ADD 	DL,30H
		MOV 	AH,02H
		INT 	21H
		LEA 	BX,HTAB
		MOV 	AL,CL
		XLAT
		MOV 	DL,AL
		ADD 	DL,30H
		MOV 	AH,02H 
		INT 	21H
		MOV		AH,4CH
		INT		21H
CSEG	ENDS
		END 	START