DSEG 	SEGMENT
ADR 	DW 		73A5H,924BH
DSEG	ENDS
CSEG 	SEGMENT
		ASSUME 	CS:CSEG,DS:DSEG
START:	MOV 	AX,DSEG
		MOV 	DS,AX
		MOV 	AX,ADR
		AND 	AX,AX	;置标志
		JNS 	PLUS	;正转
		MOV 	AX,ADR+2
		AND 	AX,AX
		JS 		NSAME
UNSAME:	MOV 	DL,'*'
		MOV 	AH,02H
		INT 	21H
		JMP 	END1
PLUS:	TEST 	ADR+2,8000H
		JS 		UNSAME
PSAME:	MOV 	DL,'+'
		MOV 	AH,02H
		INT 	21H
		JMP 	END1
NSAME:	MOV 	DL,'-'
		MOV 	AH,02H
		INT 	21H
		JMP 	END1
END1:	MOV 	AH,4CH
		INT 	21H
CSEG 	ENDS
		END 	START

