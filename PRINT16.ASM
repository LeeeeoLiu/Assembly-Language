		PUBLIC	P16
P16 	PROC 	NEAR
		MOV 	DX,AX
		MOV 	AL,AH
		XOR 	AH,AH
		MOV 	CL,16
		DIV 	CL
		MOV 	CX,AX
		LEA 	BX,HTAB
		XLAT 	
		MOV 	CL,AL
		MOV 	AL,AH
		XLAT
		MOV 	BX,DX
		MOV 	CH,AL
		MOV   	DL,CL
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,CH
		MOV 	AH,02H
		INT 	21H
		MOV 	AL,BL
		XOR 	AH,AH
		MOV 	CL,16
		DIV 	CL
		MOV 	CX,AX
		LEA 	BX,HTAB
		XLAT 	
		MOV 	CL,AL
		MOV 	AL,AH
		XLAT
		MOV 	CH,AL
		MOV   	DL,CL
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,CH
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,'H'
		MOV 	AH,02H
		INT 	21H
		RET 
P16 	ENDP