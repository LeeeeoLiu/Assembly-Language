DSEG 	SEGMENT
DATA1	DB 		'+35#'
DATA2 	DB		'+95#'
HTAB 	DB 		'0123456789ABCDEF'
DSEG 	ENDS
SSEG 	SEGMENT
STACK	DB 		50 DUP (0)
SSEG 	ENDS
CSEG 	SEGMENT
		ASSUME 	CS:CSEG,DS:DSEG
		ASSUME 	SS:SSEG
START:	MOV 	AX,DSEG
		MOV 	DS,AX
		MOV 	AX,SSEG
		MOV 	SS,AX
		MOV 	SP,SIZE STACK
		LEA 	SI,DATA1
		CALL 	DTB
		PUSH 	CX
		LEA 	SI,DATA2
		CALL 	DTB
		POP 	AX
		ADD 	AL,CL
		JO 		OVERFL
		CALL 	OUTP
		JMP 	OVER0
OVERFL:	CALL 	OVERF
OVER0:	MOV 	AH,4CH
		INT 	21H
		INCLUDE	EP821.ASM
		INCLUDE	EP822.ASM
		INCLUDE	EP823.ASM
CSEG	ENDS
		END 	START