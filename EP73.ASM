DSEG 	SEGMENT
A		DB 		3,-1,-2,10,9,-5
B		DB 		1,5,-7,-2,3,9,-10
ACNT	DB		6
BCNT	DB		7
SUM 	DB 		?
DSEG 	ENDS
SSEG 	SEGMENT
SKTOP	DB 		60 DUP (0)
SSEG 	ENDS
CSEG	SEGMENT
		ASSUME 	CS:CSEG,DS:DSEG
		ASSUME 	SS:SSEG
START:	MOV 	AX,DSEG
		MOV 	DS,AX
		MOV 	AX,SSEG
		MOV 	SS,AX
		MOV 	SP,SIZE SKTOP
		LEA 	DI,A
		XOR 	CH,CH
		MOV 	CL,ACNT
		CALL 	PLUSA
		MOV 	AX,DX
		XOR 	DX,DX
		LEA 	DI,B
		XOR 	CH,CH
		MOV 	CL,BCNT
		CALL 	PLUSA
		ADD 	AX,DX
		MOV 	SUM,AL
END0:	MOV 	AH,4CH
		INT 	21H
		INCLUDE	EP731.ASM 
CSEG 	ENDS
		END 	START
