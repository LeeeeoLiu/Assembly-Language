		PUBLIC 	OUTPUT
OUTPUT	PROC 	NEAR
		PUSH 	DI
		LEA 	BX,TAB
OUT1:	MOV	 	CX,[BX]
		XOR 	DX,DX
		DIV 	CX
		ADD 	AL,30H
		CMP 	AL,'0'
		JE 		CONTI
		PUSH 	DX
		PUSH 	AX
		MOV 	DL,AL
		MOV 	AH,02H
		INT 	21H
		POP 	AX
		POP 	DX
CONTI:	MOV 	AX,DX
		ADD 	BX,2
		CMP 	CX,10
		JNZ 	OUT1
		ADD 	AL,30H
		PUSH 	DX
		PUSH 	AX
		MOV 	DL,AL
		MOV 	AH,02H
		INT 	21H
		POP 	AX
		POP 	DX
		POP 	DI
		RET
OUTPUT 	ENDP