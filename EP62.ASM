SSEG 	SEGMENT STACK
SKTOP 	DB		40 DUP(0)
SSEG 	ENDS
DSEG 	SEGMENT
HTAB 	DB 		'0123456789ABCDEF'
DSEG 	ENDS
CSEG	SEGMENT
		ASSUME 	CS:CSEG,DS:DSEG
		ASSUME 	SS:SSEG
JMUL 	PROC 	NEAR
		AND 	BX,BX
		JZ 		JMUL1
		PUSH 	BX
		DEC 	BX
		CALL 	JMUL
F:		POP 	BX
		MUL 	BX
		RET 
JMUL1:	MOV 	AX,1
		RET 
JMUL 	ENDP
START:	MOV 	AX,DSEG
		MOV 	DS,AX
		MOV 	AX,SSEG
		MOV 	SS,AX
		MOV 	SP,SIZE SKTOP
		MOV 	AH,01H
		INT 	21H
		SUB 	AL,30H
		XOR 	AH,AH
		MOV 	BX,AX
		CALL 	JMUL
		

		MOV 	AH,4CH
		INT 	21H
		; INCLUDE JMUL.ASM	
CSEG 	ENDS
		END 	START