		PUBLIC 	MULN
MULN 	PROC 	NEAR
		CMP 	CX,0
		DEC 	CX
		JE 		PRINT0
		MOV 	DX,AX
MULN1:	MUL 	DL
		LOOP 	MULN1
		MOV 	DX,AX
		MOV 	AL,AH
		XOR 	AH,AH
		MOV 	CL,16
		DIV 	CL
		MOV 	CX,AX
		LEA 	BX,HTAB
		XLAT 	
		MOV 	CL,AL
		MOV 	AL,AH
		XLAT
		MOV 	BX,DX
		MOV 	CH,AL
		MOV   	DL,CL
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,CH
		MOV 	AH,02H
		INT 	21H
		MOV 	AL,BL
		XOR 	AH,AH
		MOV 	CL,16
		DIV 	CL
		MOV 	CX,AX
		LEA 	BX,HTAB
		XLAT 	
		MOV 	CL,AL
		MOV 	AL,AH
		XLAT
		MOV 	CH,AL
		MOV   	DL,CL
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,CH
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,'H'
		MOV 	AH,02H
		INT 	21H
		JMP 	RETURN
PRINT0:	MOV 	DL,30H
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,31H
		MOV 	AH,02H
		INT 	21H
		MOV 	DL,'H'
		MOV 	AH,02H
		INT 	21H
RETURN:	RET 
MULN	ENDP